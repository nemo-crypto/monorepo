import { Title, Text } from '@xatom/typo'
import { Cols, Stacks, Cell, Fluid, Scroll, Auto } from '@xatom/flex'
import { Button, Icon, Form, Field, Select, Input, NumberInput, toast, notify, Spin, Tag, Tabs, Tooltip } from '@xatom/antd'
export {
  Title, Text,
  Cols, Stacks, Cell, Fluid, Scroll, Auto,
  Button, Icon, Spin,
  Form, Field, Select, Input, NumberInput,
  toast, notify,
  Tag,
  Tabs,
  Tooltip,
}
